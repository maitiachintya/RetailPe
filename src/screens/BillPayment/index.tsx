import { View, Text } from 'react-native';
import React from 'react';

const BillPayment = () => {
  return (
     <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
          <Text style={{fontSize: 30, fontWeight: 'bold'}}>Bill Payment</Text>
        </View>
  );
};

export default BillPayment;
