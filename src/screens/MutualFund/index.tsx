import { View, Text } from 'react-native';
import React from 'react';

const MutualFund = () => {
  return (
     <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
          <Text style={{fontSize: 30, fontWeight: 'bold'}}>Mutual Fund</Text>
        </View>
  );
};

export default MutualFund;
